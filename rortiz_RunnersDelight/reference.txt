```

### Step 2: Update `RunService` to Command the New `CoreMotionManager`

Now that our `CoreMotionManager` is perfect, we need to make sure `RunService` is using the correct methods to command it. Specifically, we need to call `pauseUpdates()` when the run is paused, not a generic `stop` or `reset`.

Here are the required changes for `RunService.swift`:

```swift
// filename: RunService.swift (updating the lifecycle methods)

// ... (inside your RunService class)

func start() {
    guard case .inactive = state else { return }
    
    state = .running(startTime: Date(), accumulatedTime: 0)
    
    startDurationTimer()
    coreMotionManager.startUpdates() // This is correct
    
    print("▶️ RunService: Run started.")
}

func pause() {
    guard case .running(let startTime, let accumulatedTime) = state else { return }
    
    let newAccumulatedTime = accumulatedTime + Date().timeIntervalSince(startTime)
    state = .paused(accumulatedTime: newAccumulatedTime)
    
    // --- UPDATE THIS LINE ---
    coreMotionManager.pauseUpdates() // Use the new, specific pause method
    
    print("⏸️ RunService: Run paused.")
}

func resume() {
    guard case .paused(let accumulatedTime) = state else { return }
    
    state = .running(startTime: Date(), accumulatedTime: accumulatedTime)
    liveRunData.routeSegments.append(RouteSegment())
    
    coreMotionManager.startUpdates() // This is correct for resuming
    
    print("▶️ RunService: Run resumed.")
}

func end() {
    // ... (your existing switch statement for final duration calculation)
    
    // --- Perform all cleanup AFTER the final calculation ---
    
    stopDurationTimer()
    
    // --- UPDATE THIS LINE ---
    coreMotionManager.reset() // Use the reset method for a full stop and cleanup
    
    state = .inactive
    cancellables.forEach { $0.cancel() }
    cancellables.removeAll()
    currentLocation = nil
    previousLocation = nil
    
    print("⏹️ RunService: Run ended and cleaned up.")
}

